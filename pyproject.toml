[project]
name = "habit-tracker"
version = "0.1.0"
description = "Habit tracking app"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.2.3",
    "requests==2.32.4",
]
